A <- c(1, 2, 3, 4, 5, 6, 7, 8)

#Range will return the in and max values in the vector 
res <- range(A)
print(res)

#to get the difference between the max and the min, you can use 
print(dff(res))

temp <- nottem 
print(range(temp))

print(quantile(A))

InterQuantilValue <- IQR(A)
print(InterQuantileValue)